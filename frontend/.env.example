# Copy to .env to override defaults (optional for local dev)
# Docker: VITE_API_URL is passed as a build arg in docker-compose.yml
# Remote deploy: set this to the backend's public URL before building
VITE_API_URL=http://localhost:8000
