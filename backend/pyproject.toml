[tool.bandit]
# B608 (hardcoded SQL): every SQL statement in this codebase uses either
# hardcoded table-name constants from a fixed list (not user input) or
# parameterized queries with '?' placeholders. No user-controlled strings
# are interpolated into SQL.
#
# B108 (hardcoded /tmp): /tmp is intentionally used as the sandbox working
# directory to isolate untrusted code execution. This is the secure choice,
# not a vulnerability.
skips = ["B608", "B108"]
